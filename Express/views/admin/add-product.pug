extends ../layouts/main-layout.pug 
block styles
    link(rel="stylesheet", href="")
block content
    main
        p Above line (line 1) is used for extending the main-layout which is having the nav bar, we can say it as master template where we put all the common layouts
        p In below line (line 2 ) we can use custom css for this particular page, and master css will get imported from main-layout.pug
        form(action="/admin/add-product", method="POST")
            .form-control
                label(for="title") Title
                input(type="text" name="title" class="title")#title
                label(for="imageUrl") Img
                input(type="text" name="imageUrl")#imageUrl
                label(for="price") Price
                input(type="number" name="price" step="0.01")#pirce
                label(for="description") Description
                textarea(name="description", cols="5")#description
            button.btn(type="submit") Add Product